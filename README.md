# Conditional-VAE-for-CelebA-Image-Manipulation
how to build and train a Conditional Variational Autoencoder (CVAE) model using TensorFlow and the CelebA dataset. The CVAE can be used to generate images with specific attributes and modify existing images by changing their attributes.
